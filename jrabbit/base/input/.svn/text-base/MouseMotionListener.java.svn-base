package org.jrabbit.base.input;

/*****************************************************************************
 * A MouseMotionListener is an interface used to provide automatic notification
 * of Mouse movement.
 * 
 * To receive notifications, add a listener to MouseHandler. If the object
 * receiving updates is no longer being used, remove it from the MouseHandler.
 * 
 * NOTE: A MouseMotionListener does not receive updates about mouse button
 * events. To receive those, look at MouseListener.
 * 
 * @author Chris Molini
 *****************************************************************************/
public interface MouseMotionListener
{
	/*************************************************************************
	 * Notifies the MouseMotionListener when the mouse cursor is moved.
	 * 
	 * NOTE: +Y is down the screen.
	 * 
	 * @param deltaX
	 *            The amount the mouse moved on the x-axis.
	 * @param deltaY
	 *            The amount the mouse moved on the y-axis.
	 *************************************************************************/
	public void mouseMoved(int deltaX, int deltaY);
}
